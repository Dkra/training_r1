const app = require('../app')
const request = require('supertest')
/*
describe('Testing Login APIs', () => {
	describe('GET /login', () => {
		it('should return status 200', function(done) {
			request(app)
				.get('/login')
				.expect(200)
				.end(function(err, res) {
					if (err) return done(err)
					done()
				})
		})
	})

	describe('POST /api/login', function(done) {
		it('should return 401 with incorrect Username & Password', function(done) {
			request(app)
				.post('/api/login')
				.send({
					username: 'admin',
					password: 'wrongPassword'
				})
				.expect(401)
				.end(function(err, res) {
					if (err) return done(err)
					done()
				})
		})
	})
})
*/
